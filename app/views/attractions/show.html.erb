<%= @attraction.name %>
<%= button_to "Go on this ride", { action: "ride" }, id: "button" unless current_user.admin %>
<%= link_to "Edit Attraction", edit_attraction_path(@attraction) if current_user.admin %>